# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - vmori-dev-client

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Log in to registry
      uses: docker/login-action@v1
      with:
        registry: https://vmori.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_bca84fa877254f93b44b5e097f6956e1 }}
        password: ${{ secrets.AzureAppService_ContainerPassword_2458653f90434884b918546075b21576 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v2
      with:
        push: true
        tags: ${{ secrets.AzureAppService_ContainerUsername_bca84fa877254f93b44b5e097f6956e1 }}/vmori/vue}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'vmori-dev-client'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_a9fe9047a0344d6fb556fffc37f4327e }}
        images: 'vmori.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_bca84fa877254f93b44b5e097f6956e1 }}/vmori/vue:${{ github.sha }}'